apiVersion: apps/v1
kind: Deployment
metadata:
  name: hydrowatch-backend
  namespace: hydrowatch
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hydrowatch-backend
  template:
    metadata:
      labels:
        app: hydrowatch-backend
    spec:
      imagePullSecrets:
        - name: dockerhub-secret
      containers:
        - name: hydrowatch-backend
          image: DOCKERHUB_USERNAME/hydrowatch-backend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hydrowatch-frontend
  namespace: hydrowatch
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hydrowatch-frontend
  template:
    metadata:
      labels:
        app: hydrowatch-frontend
    spec:
      imagePullSecrets:
        - name: dockerhub-secret
      containers:
        - name: hydrowatch-frontend
          image: DOCKERHUB_USERNAME/hydrowatch-frontend:latest
          ports:
            - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: hydrowatch-backend
  namespace: hydrowatch
spec:
  selector:
    app: hydrowatch-backend
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: hydrowatch-frontend
  namespace: hydrowatch
spec:
  selector:
    app: hydrowatch-frontend
  ports:
    - port: 80
      targetPort: 80
  type: LoadBalancer
